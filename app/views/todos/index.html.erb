<h1>To Do list </h1>

<% @todos.each do |todo| %>
 <div class="well">
 	<%= todo.description %>
 	<%= todo.completed %>
 	<%= link_to 'Show', todo, :class => "btn btn-primary"%>
 </div>
<% end %>

<%= link_to "Crear ToDo", todos_new_path, :class => "btn btn-success" %>